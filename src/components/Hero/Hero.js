import React from "react";

import "./Hero.scss";

const Hero = () => {
  return (
    <div className="hero">
      <span className="hero__title">Watch Something Incredible.</span>
    </div>
  );
};

export default Hero;
